import { StyleSheet } from "react-native";

const CommonStyles = StyleSheet.create({
    InfoText:{
        margin: 10,
        color: '#ffffff',
        fontSize: 18,
        alignSelf: 'center',
        opacity:0.5
    },
})

export default CommonStyles;